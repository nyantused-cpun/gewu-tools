// gewu brief-preset：community（默认/现状行为，零注入）
// 契约 v1：纯数据对象，禁止副作用（不读文件、不联网、不引依赖）。
export default {
  name: "community",
  version: "1",
  defaultFocus: null,
  sections: [],
  dispatchHints: null,
  requires: {},
};